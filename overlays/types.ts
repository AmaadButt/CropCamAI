export type OverlayProps = {
  width: number;
  height: number;
  color: string;
  opacity: number;
  thickness: number;
  animate?: boolean;
  safeInsets: { top: number; bottom: number; left: number; right: number };
};
